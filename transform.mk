# Add a new stopify transform here. The name should correspond to the flag
# used by the stopify binary. Use '@' to represent spaces when specifying
# options.
TRANSFORMS = yield yield@--optimize

# Specify the intervals here.
INTERVALS = 1000 2000
# 500 750 1000 1250 1500 2000 2500
